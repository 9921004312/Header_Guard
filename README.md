# Header_Guard
Installation-Prerequisites

     python --version
Install dependencies

     pip install playwright

     python -m playwright install chromium

     pip install pandas
Notes:

Playwright: Captures real browser response headers (DevTools‑equivalent)

Chromium: At least one Playwright browser is required

pandas + openpyxl: Read/write .xlsx input and output files
